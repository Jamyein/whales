<!DOCTYPE html>
<html lang="<%= config.language %>">
<head>
    <%-partial('_partials/head')%>
</head>

<body class="<%= page.layout || (is_home() ? 'home' : '') %>">
<div class="main" id="page-container">
    <%-partial('_partials/header')%>
    <div id="content">
        <%- body %>
    </div>
    <footer>
    <%-partial('_partials/foobar', {}, {cache: true})%>
    </footer>
</div>

</body>
</html>
